/**
 * Team module: teams, team membership, and team-level roles (OWNER / ADMIN / MEMBER).
 * Owns the {@code teams} and {@code team_members} tables and enforces team-level
 * authorization rules (e.g. only an OWNER may delete a team).
 */
package com.collabflow.team;
