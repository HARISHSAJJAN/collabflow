/**
 * User module: user profile data (owns the {@code users} table), profile read/update, and
 * the {@code UserDirectory} named interface used by other modules to resolve a user id to
 * display information (name, avatar) without depending on this module's internals.
 */
package com.collabflow.user;
